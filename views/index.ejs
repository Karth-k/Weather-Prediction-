<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aeolus-Weather app</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css" >
</head>
<body>
    <div class="container">
<!-- By default, there is no input city so the file may throw an error, to prevent this we use an 
  if-else block which displays null value when the site is first opened -->
        <header>


          <h2 class="aeolus"> Aeolus-Feel the Wind</h2>
        </header>
         
        
      
        <div class="cover">
          <h1>Discover how it's out there.</h1>
          <form  class="flex-form" action="/" method="post">
            <label for="from">
              <i class="ion-location"></i>
            </label>
            <input type="text" name="city" placeholder="Where do you want to go?">
            <input type="submit" value="Get Weather">
          </form>
          <div class="wrapper">
            <div class="widget-container">
              <div class="top-left">
                <h1 class="city" id="city"></h1>
                <h1 id="day"><%if(weather!==null){%>
                  <p><%=weather.shahar%></p>
              <%}%></h2>
                <h3 id="date"><p><%if(weather!==null){%>
                  <p><%=weather.date%></p>
              <%}%></p></h3>

                <!-- <h3 id="time"><p></p></h3> -->
          <!--       <a target="_blank" href="https://codepen.io/myleschuahiock/"><p id="codepen-link">codepen.io/myleschuahiock</p></a> -->
                <p class="geo"></p>
              </div>
              <div class="top-right">
                <h1 id="weather-status">Weather / Weather Status</h1>
                <img class="weather-icon" <%if(weather!==null){%>
                  src="https://openweathermap.org/img/wn/<%= weather.icons%>@2x.png"
              <%}%>>
              </div>
              <div class="horizontal-half-divider"></div>
              <div class="bottom-left">
                <h1 id="temperature">
                <!-- <h2 id="celsius"> &degC</h2>
                <h2 id="temp-divider">/</h2>
                <h2 id="fahrenheit">&degF</h2> -->
                <p><%if(weather!==null){%>
                  <p><%=weather.temp%></p>
              <%}%></p>
              </div>
              <div class="vertical-half-divider"></div>
              <div class="bottom-right">
                <div class="other-details-key">
                  <p>Wind Speed</p>
                  <p>Humidity</p>
                  <p>Pressure</p>
                </div>
                <!-- <div class="other-details-values">
                  <p class="windspeed">Km/h</p>
                  <p class="humidity">g/kg</p>
                  <p class="pressure"> hPa</p>

                </div> -->
                <div class="other-details-values">
                  <% if (weather && weather.wind !== null) { %>
                    <p class="windspeed"><%= weather.wind %> Km/h</p>
                  <% } else { %>
                    <p class="windspeed">N/A</p>
                  <% } %>
                  <% if (weather && weather.humidity !== null) { %>
                    <p class="humidity"><%= weather.humidity %> Km/h</p>
                  <% } else { %>
                    <p class="humidity">N/A</p>
                  <% } %>
                  <% if (weather && weather.pressure !== null) { %>
                    <p class="pressure"><%= weather.pressure %> Km/h</p>
                  <% } else { %>
                    <p class="pressure">N/A</p>
                  <% } %>

                </div>
                
              </div>
            </div>
          </div>
      </div>


        </div>
      
    
</body>
</html>